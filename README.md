# 依恋型风格测试

一个完全静态、可独立运行的依恋风格测试备份版本。包含 30 道题、五档选项、四维评分、主/次类型组合和详细结果解析。

## 特点

- 不依赖后端接口或数据库
- 所有题目与评分均在浏览器本地完成
- 支持手机和桌面浏览器
- 可直接部署到 GitHub Pages

## 本地预览

直接打开 `index.html`，或在此目录运行：

```bash
python -m http.server 8000
```

然后访问 `http://localhost:8000/`。

## 说明

测试结果仅用于自我观察和交流，不构成心理诊断。

